// src/index.js

// This is the main entry point of the package.
// Here you can export the primary functionality of your package.

export const greet = (name) => {
    return `Hello, ${name}!`;
};

// You can add more functions or classes as needed.